# =============================================================================
# Data Miner Environment Variables
# Copy to .env and fill in values
# =============================================================================

# Hugging Face token for private models (optional)
# Get from: https://huggingface.co/settings/tokens
HF_TOKEN=

# Database URL (defaults to local postgres)
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/data_miner

# Debug mode - disables heartbeat (for debugging only!)
# DATA_MINER_DEBUG=1

# =============================================================================
# Distributed Deployment (worker nodes only)
# =============================================================================

# Master node IP - workers use this to connect to master's services
# Required when running docker-compose.worker.yml
MASTER_IP=192.168.1.100

# Local disk path for SeaweedFS volume data (worker nodes)
LOCAL_DISK=/data/seaweed

# Loki URL for centralized logging (optional, defaults to http://${MASTER_IP}:3100)
# LOKI_URL=http://192.168.1.100:3100/loki/api/v1/push
